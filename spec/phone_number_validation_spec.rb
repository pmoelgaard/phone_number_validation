require 'spec_helper'

describe PhoneNumberValidation do
  it 'has a version number' do
    expect(PhoneNumberValidation::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
